<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Books - Library</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex flex-col items-center p-10">

    <h1 class="text-4xl font-bold text-center text-gray-400 mb-8">Search result</h1>

    <!-- Back to Home Button -->
    <a href="/Member" class="text-blue-600 hover:text-white transition mb-10">← Back to Home</a>

    <!-- Books Section -->
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-6">
        <% if (books.length > 0) { %>
            <% books.forEach(function(book) { %>
                <!-- Book Card -->
                <div class="bg-zinc-800 p-4 rounded-lg shadow-lg transition-transform transform hover:scale-105">
                    <!-- Book Image -->
                    <div class="h-48 w-full bg-gray-700 rounded-lg overflow-hidden mb-4">
                        <img class="w-full h-full object-cover" src="<%= book.image %>" alt="Book Cover">
                    </div>
                    <!-- Book Details -->
                    <h3 class="text-lg font-semibold mb-1">Title: <%= book.title %></h3>
                    <p class="text-gray-400">Author: <%= book.author %></p>
                    <p class="text-gray-400">ISBN: <%= book.ISBN %></p>
                    <p class="text-gray-400">Copies: <%= book.copies %></p>
                    <p class="text-gray-400">Rack Number: <%= book.rackno %></p>
                </div>
            <% }) %>
        <% } else { %>
            <p class="text-gray-500 text-center w-full">No Books Available Yet!</p>
        <% } %>
    </div>

</body>
</html>
